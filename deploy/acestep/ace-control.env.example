# Required; replace with a random secret (at least 32 characters).
ACESTEP_CONTROL_TOKEN=
ACESTEP_LOCAL_URL=http://127.0.0.1:8001
# Same API key as ACE's acestep.env. Never expose it to the browser.
ACESTEP_API_KEY=
ACESTEP_CONTROL_LLM_URL=http://127.0.0.1:1234/api/v0/models
