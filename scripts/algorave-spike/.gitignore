# The repo root only ignores web/frontend/node_modules, so this spike ignores
# its own. package-lock.json is deliberately NOT ignored: the whole point of the
# spike is a reproducible pin of the @strudel/* tree.
node_modules/
